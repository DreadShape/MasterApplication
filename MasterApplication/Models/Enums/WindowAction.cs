namespace MasterApplication.Models.Enums;

/// <summary>
/// <see cref="Enum"/> to either show, hide or maximize the window.
/// </summary>
internal enum WindowAction
{
    Normal,
    Minimize,
    Maximize
}
