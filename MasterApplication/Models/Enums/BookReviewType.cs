namespace MasterApplication.Models.Enums;

/// <summary>
/// <see cref="Enum"/> for the different book reviews that we can make. Each type will have subtle differences that need to be taken into account.
/// </summary>
public enum BookReviewType
{
    GoodReads,
    Hardcover
}
