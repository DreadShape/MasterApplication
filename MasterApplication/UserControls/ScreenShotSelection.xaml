<Window x:Class="MasterApplication.UserControls.ScreenShotSelection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:MasterApplication.UserControls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="ScreenShotSelection"
        d:DesignHeight="450"
        d:DesignWidth="800"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseMove="Window_MouseMove"
        WindowState="Maximized"
        WindowStyle="None"
        mc:Ignorable="d">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition />
    </Grid.RowDefinitions>

    <!-- Screenshot Image -->
    <Image x:Name="ScreenshotImage" Stretch="Fill" />

    <!-- Semi-transparent overlay -->
    <Canvas x:Name="OverlayCanvas" Background="#80000000">
      
      <!-- Selection Rectangle (placed outside the overlay) -->
      <Rectangle x:Name="SelectionRectangle"
                 Fill="Transparent"
                 Stroke="DodgerBlue"
                 StrokeThickness="2"
                 Visibility="Collapsed" />
    </Canvas>

  </Grid>
</Window>
